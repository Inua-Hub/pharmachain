import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PharmaChain - Pharmaceutical Supply Chain Management',
  description: 'Secure pharmaceutical supply chain management on blockchain',
  keywords: 'pharmaceutical, supply chain, blockchain, medicine verification, TMDA, Tanzania',
  authors: [{ name: 'PharmaChain Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}